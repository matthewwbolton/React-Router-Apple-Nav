import React from 'react';
import appleData from '../appleData';
import { Link } from 'react-router-dom';


const Card = (props) => {
    
    
    return (
        <div>{props.elem}</div>
    );
};

export default Card;